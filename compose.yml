services:
  certbot-kubectl:
    build: 
      context: .
      dockerfile: Dockerfile
      tags:
      - certbot-kubectl:latest
      - certbot-kubectl:1.0.0
      - ghcr.io/bluestep-systems/certbot-kubectl:latest
      - ghcr.io/bluestep-systems/certbot-kubectl:1.0.0
    image: certbot-kubectl